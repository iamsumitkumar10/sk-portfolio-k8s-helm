apiVersion: v1
kind: Service
metadata:
  name: {{ include "sk-portfolio.fullname" . }}
  labels:
    {{- include "sk-portfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sk-portfolio.selectorLabels" . | nindent 4 }}
